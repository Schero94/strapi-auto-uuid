'use strict';

import { Key } from '@strapi/icons';

/**
 * Plugin Icon Component
 * 
 * Displays a key icon representing UUID/unique identifier functionality.
 * Uses Strapi's design system icons for consistency.
 */
export const PluginIcon = () => <Key />;

export default PluginIcon;
